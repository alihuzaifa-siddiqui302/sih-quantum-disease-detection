[pytest]
addopts = -m "not slow"
markers =
    slow: marks tests as slow (deselect with '-m "not slow"'; run with '-m slow')
filterwarnings =
    ignore::DeprecationWarning:qiskit.*
    ignore::DeprecationWarning:qiskit_machine_learning.*
    ignore::PendingDeprecationWarning:qiskit.*
